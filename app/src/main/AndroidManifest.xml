<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myhealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.medical.ActivationActivity"></activity>
        <activity android:name=".ui.lifestyle.GoalNoteLogActivity" />
        <activity android:name=".ui.lifestyle.WeightLogActivity" />
        <activity android:name=".ui.lifestyle.ExerciseLogActivity" />
        <activity android:name=".ui.lifestyle.FoodWaterLogActivity" />
        <activity android:name=".ui.medical.ClinicLocatorActivity" />
        <activity android:name=".ui.medical.MessengerActivity" />
        <activity android:name=".ui.medical.TestResultsActivity" />
        <activity android:name=".ui.medical.PatientChartActivity" />
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>